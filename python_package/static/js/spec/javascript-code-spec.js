/**
 * Tests for studio edit's utils
 */
/* global addIt */

describe('exspression: 1+1', function() {
    'use strict';

    it('return', function() {
        expect(addIt(1, 1)).toEqual(2);
    });
});
